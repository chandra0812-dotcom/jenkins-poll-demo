pipeline {
    agent any

    stages {
        stage('Checkout Info') {
            steps {
                echo "Polling SCM Demo Running..."
                sh 'date'
                sh 'cat message.txt'
            }
        }
    }
}
